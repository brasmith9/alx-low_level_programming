#include "main.h"

/**
 * add - add two integer params
 * @firstNumber: first number to add
 * @secondNumber: second number to add
 * Return: int
 */
int add(int firstNumber, int secondNumber)
{
	return (firstNumber + secondNumber);
}
